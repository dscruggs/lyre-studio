{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r public/* dist/ && bun build ./src/main.jsx --outdir ./dist --minify",
    "dev": "bun run build && bunx serve dist -l 5173",
    "dev:watch": "bun build ./src/main.jsx --outdir ./dist --watch",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase": "^10.8.1",
    "lucide-react": "^0.555.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "globals": "^16.5.0",
    "serve": "^14.2.4"
  }
}